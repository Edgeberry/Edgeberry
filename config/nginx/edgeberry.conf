# Edgeberry platform — http-level directives
# Loaded by nginx.conf's include of conf.d/*.conf (inside http{}).

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

underscores_in_headers on;
